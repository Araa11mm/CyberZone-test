import Header from '@components/Header'
import Authorization from '@components/Authorization';
const Auth = () => {
  return (
    <div>
      <Header />
      <Authorization />
    </div>
  )
}

export default Auth
